# Frontend — React + Vite + Tailwind

See the root `README.md` and `DEPLOYMENT.md` for setup and hosting instructions.

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # production build in dist/
```

Set `VITE_API_URL` (see `.env.example`) to point at your backend.
